function distanceReadings = ReadDistances(x, y, orientation, envir, C)
   distanceReadings = zeros(1, C.ANGLES, 'uint8');
   % calculate distances based on obstacle definitions and angle that we
   % are pointing
end